<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/head %>
</head>

<body>
    <div class="container">
        <header>
        </header>
        <main>
            <h2><%= event.eventName %></h2>
            <ul>
                <li><%= event.djName %></li>
                <li><%= event.location %></li>
                <li><%= event.attendees %></li>
                <li><%= event.eventDate %></li>
            </ul>
            <form action="/song-requests/<%= event._id%>" method="GET">
                <div class="form-group">
                    <button type="submit" class="btn btn-default">Join!</button>
                </div>
            </form>
            <form action="/song-requests/<%= event._id%>" method="POST">
                <div class="form-group">
                    <button type="submit" class="btn btn-default" onclick="youDidIt()">RSVP</button>
                </div>
            </form>
            
        </main>
    </div>

    <% include ../partials/scripts %>
</body>

</html>